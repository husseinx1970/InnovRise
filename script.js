function draw() {
    // Immediate drawing of the company name
    context.clearRect(0, 0, canvas.width, canvas.height);
    // Assuming 'drawCompanyName' is the function that draws the name
    drawCompanyName();
    // ... other drawing logic if needed
}